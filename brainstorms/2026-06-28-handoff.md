# Session Handoff — 2026-06-28

Quick-start for a fresh chat. Read this + `CLAUDE.md` + `brainstorms/2026-06-27-blank-platform-vision.md` (the ★ FINAL CONSOLIDATED DECISIONS block), then continue.

## Where things stand
- **Pivot complete:** Blank = a consumer AI-editor news reader (PWA). The old 3x/day carousel scout is retired. Full vision in the platform brainstorm; engine/product facts in `CLAUDE.md`.
- **Phase 1 SHIPPED & live on `main`:**
  - Feed leads with **real headlines** (AI hook/why generation removed).
  - **Per-source cap = 15** (`db.PER_SOURCE_CAP`) — bounds triage/scoring cost.
  - **X + Google trends** re-added (`ingest.fetch_trends`), throttled to **3h** (`TREND_REFRESH_MINUTES = 180`) to stay under Apify's free $5/mo tier; graceful no-op if `APIFY_API_TOKEN` unset.
  - **Feed window = 48h** (`deploy-pages.yml` + `db.get_feed`), matching ingest cutoff.
  - **Time-decayed ranking moved into the engine** — `db.DECAY_HOURS_PER_POINT = 12` + `db.decayed_rank()` are the single source of truth; `deploy-pages.yml` emits `picks_data.json` already ranked; `index.html` just renders the given order.
  - **UI redesign** (Apple-News style, rarity gems, light/dark, 5-tab shell) shipped in `index.html` (commit `411b58f`); details in `brainstorms/2026-06-27-blank-ui-redesign.md`.
- **Engine health:** recovered after an Anthropic-credit outage. Credits reloaded 2026-06-27. The first run after reload cleared a ~620-item backlog (one-time, expected — items escalated-but-unscored during the outage). **BASELINE (confirmed good 2026-06-28):** at steady state "items to score" sits in the tens — that's healthy. If it ever creeps back into the hundreds, tune `PER_SOURCE_CAP` down or prune the highest-volume Google News feeds (usual cause: a new high-volume source or a big trends batch).

## NEXT build (immediate)
- **Topic tagging — SHIPPED (2026-06-28).** Sonnet now tags each item *in the existing score call* (no extra LLM pass): a fixed `primary_category` (15 curated buckets + `Other`) plus free-form `tags`. `score.CATEGORIES` is the source of truth + injected into the prompt; `score._normalize_category()` / `_normalize_tags()` sanitize the output; stored in new `scores.primary_category` + `scores.tags` columns (idempotent `init_db()` migration). `db.record_score()` + `db.get_feed()` carry both. `deploy-pages.yml` deliberately left untouched (selects only the columns it needs) so the live feed is unaffected until the PWA consumes niches.
- **Next: niche onboarding** — "topics to start, sources to go deep." The PWA's Niches tab maps user-picked niches → `primary_category`; then surface `primary_category`/`tags` into `picks_data.json` (extend the `deploy-pages.yml` query + pick dict — watch the migration-timing caveat: guard so a deploy that runs before the next engine migration doesn't drop all engine items) and add per-user niche filtering on the global ranked feed. **Still global-score-once; never per-user LLM scoring.**

## Phase 2 / 3 roadmap
- **Phase 2:** topic tagging → niche onboarding ("topics to start, sources to go deep") → automated niche→source catalog *with founder oversight* → user accounts → per-user re-rank + "this wasn't noise" correction loop. **Personalization = global score + cheap periodic per-user taste profile + near-free re-rank. NEVER per-user LLM scoring** (that's the cost explosion to avoid).
- **Phase 3:** freemium paywall + payments ($10–15/mo) → selective push → on-demand "catch me up" summaries → marketing content engine (curated-story posts as a from-scratch audience funnel; founder has ~no existing social audience).

## Open decisions (founder's to make)
- **Name:** Keen vs. Caret vs. Blank (sitting with it). Don't hardcode a final name.
- ~~**Topic-tagging shape**~~ — RESOLVED 2026-06-28: "both" (fixed primary + free-form tags), 15 buckets + `Other`. Shipped.
- **Rank-don't-hide:** deferred until the Phase 2 discovery surface exists (removing the `score>=6` filter alone would dump noise into the feed).

## Tooling
- **Notion connected (2026-06-28):** a Notion claude.ai Connector is set up (read + write). Connector tools load at session start, so they're available in a *fresh* session as `mcp__claude_ai_Notion__*`. Verify the connector is live first with a quick tool check (`fetch` id `self`).
- **Notion structure — DONE (2026-06-28):** a **Blank** parent page now holds three things in *Moeed's Notion*, populated from current state:
  - **Blank** (overview) — what it is, why it's different, the moat (global-score-once cost architecture), the engine, and all Phase 1 features shipped: <https://app.notion.com/p/38e4a56217ba816293e0c1c6108164a5>
  - **Session Log** (newest-first session entries; seeded with 2026-06-27 + 2026-06-28): <https://app.notion.com/p/38e4a56217ba815dbb41f32a688811ee>
  - **Roadmap** (database, Status × Phase) — 17 items across Shipped / Next (niche onboarding) / Parked.
  - Going forward: log each session in **Session Log** and move Roadmap items as they ship. \"Next\" = the single active build; everything future is **Parked** (no \"In Progress\" status yet — add one if you want to track active work distinctly).

## Working mechanics
- `git pull origin main` first — the engine commits `blank.db` every ~10 min, so `main` moves under you; expect to `git pull --rebase` (stash a dirty `blank.db` first) before pushing.
- Commit on `main`; **never commit `.env`**; keep `CHANGELOG.md` to 20 entries.
- Tunable cost knobs, all single constants: `DECAY_HOURS_PER_POINT` + `PER_SOURCE_CAP` (`db.py`), `TREND_REFRESH_MINUTES` (`ingest.py`), 48h windows (`deploy-pages.yml` + `db.get_feed`).
- Prompt caching is NOT viable at current prompt sizes (triage ~630 / score ~810 tokens are below the 4096/2048 cacheable minimums) — don't re-attempt it.
